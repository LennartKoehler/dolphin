@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find the dependencies that dolphin-image needs
find_dependency(TIFF REQUIRED)
find_dependency(ITK REQUIRED)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/dolphin-imageTargets.cmake")

check_required_components(dolphin-image)
