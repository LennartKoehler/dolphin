name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # -----------------------------------------------------------------
  # Prepare — extract version from git tag (strip leading 'v')
  # -----------------------------------------------------------------
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Set version
        id: version
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=0.0.1" >> "$GITHUB_OUTPUT"
          fi

  # -----------------------------------------------------------------
  # Ubuntu — Build (Docker-based, extract CLI binary)
  # -----------------------------------------------------------------
  ubuntu:
    runs-on: ubuntu-22.04
    needs: prepare
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-linux-x86_64
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.cpu
          push: false
          load: true
          tags: dolphin-release:latest
          cache-from: type=gha,scope=cpu
          cache-to: type=gha,scope=cpu,mode=max

      - name: Extract CLI binary
        run: |
          docker create --name temp dolphin-release:latest
          docker cp temp:/workspace/build/frontends/cli/dolphin_cli ./dolphin_cli
          docker rm temp
          chmod +x ./dolphin_cli
          tar czf ${{ env.ARTIFACT_NAME }}.tar.gz dolphin_cli

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.tar.gz

  # -----------------------------------------------------------------
  # Ubuntu — Test (clean VM)
  # -----------------------------------------------------------------
  ubuntu-test:
    runs-on: ubuntu-22.04
    needs: [prepare, ubuntu]
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-linux-x86_64

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Smoke test
        run: |
          tar xzf ${{ env.ARTIFACT_NAME }}.tar.gz
          chmod +x ./dolphin_cli
          output=$(./dolphin_cli --help 2>&1) || {
            echo "::error::dolphin_cli exited with code $?"
            echo "$output"
            exit 1
          }
          echo "$output" | grep -qE "psfgenerator|deconvolution" || {
            echo "::error::Unexpected help output — CLI did not start correctly"
            echo "$output"
            exit 1
          }
          echo "Smoke test passed."

  # -----------------------------------------------------------------
  # Ubuntu — Publish to GitHub Release
  # -----------------------------------------------------------------
  ubuntu-publish:
    runs-on: ubuntu-22.04
    needs: [prepare, ubuntu-test]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-linux-x86_64

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ARTIFACT_NAME }}.tar.gz

  # -----------------------------------------------------------------
  # Ubuntu CUDA — Build (Docker-based, extract CLI binary)
  # -----------------------------------------------------------------
  ubuntu-cuda:
    runs-on: ubuntu-22.04
    needs: prepare
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-linux-x86_64-cu130
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: dolphin-release-cuda:latest
          cache-from: type=gha,scope=cuda
          cache-to: type=gha,scope=cuda,mode=max

      - name: Extract CLI binary
        run: |
          docker create --name temp dolphin-release-cuda:latest
          docker cp temp:/workspace/build/frontends/cli/dolphin_cli ./dolphin_cli
          docker rm temp
          chmod +x ./dolphin_cli
          tar czf ${{ env.ARTIFACT_NAME }}.tar.gz dolphin_cli

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.tar.gz

  # -----------------------------------------------------------------
  # Ubuntu CUDA — Test (clean VM)
  # -----------------------------------------------------------------
  ubuntu-cuda-test:
    runs-on: ubuntu-22.04
    needs: [prepare, ubuntu-cuda]
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-linux-x86_64-cu130

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Smoke test
        run: |
          tar xzf ${{ env.ARTIFACT_NAME }}.tar.gz
          chmod +x ./dolphin_cli
          output=$(./dolphin_cli --help 2>&1) || {
            echo "::error::dolphin_cli exited with code $?"
            echo "$output"
            exit 1
          }
          echo "$output" | grep -qE "psfgenerator|deconvolution" || {
            echo "::error::Unexpected help output — CLI did not start correctly"
            echo "$output"
            exit 1
          }
          echo "Smoke test passed."

  # -----------------------------------------------------------------
  # Ubuntu CUDA — Publish to GitHub Release
  # -----------------------------------------------------------------
  ubuntu-cuda-publish:
    runs-on: ubuntu-22.04
    needs: [prepare, ubuntu-cuda-test]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-linux-x86_64-cu130

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ARTIFACT_NAME }}.tar.gz

  # -----------------------------------------------------------------
  # macOS — Build
  # -----------------------------------------------------------------
  macos:
    strategy:
      fail-fast: false
    runs-on: macos-14
    needs: prepare
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-macos-arm64
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Restore Homebrew cache
        id: brew-cache
        uses: actions/cache/restore@v4
        with:
          path: ~/Library/Caches/Homebrew
          key: brew-${{ runner.os }}-${{ runner.arch }}-libtiff-fftw-libomp-ccache-dylibbundler

      - name: Install dependencies via Homebrew
        run: |
          brew install libtiff fftw libomp ccache dylibbundler

      - name: Save Homebrew cache
        if: steps.brew-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ~/Library/Caches/Homebrew
          key: ${{ steps.brew-cache.outputs.cache-primary-key }}

      - name: Restore ITK cache
        id: cache-itk
        uses: actions/cache/restore@v4
        with:
          path: ~/itk-install
          key: itk-5.4.5-notiff-static-${{ runner.os }}-${{ runner.arch }}

      - name: Build ITK from source
        if: steps.cache-itk.outputs.cache-hit != 'true'
        run: |
          wget https://github.com/InsightSoftwareConsortium/ITK/releases/download/v5.4.5/InsightToolkit-5.4.5.tar.gz
          tar -xzf InsightToolkit-5.4.5.tar.gz
          cd InsightToolkit-5.4.5
          mkdir ITK-build && cd ITK-build
          cmake -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_INSTALL_PREFIX=$HOME/itk-install \
                -DBUILD_TESTING=OFF \
                -DBUILD_EXAMPLES=OFF \
                -DBUILD_SHARED_LIBS=OFF \
                -DModule_ITKIOTIFF=OFF \
                ..
          make -j$(sysctl -n hw.ncpu)
          make install

      - name: Save ITK cache
        if: steps.cache-itk.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ~/itk-install
          key: ${{ steps.cache-itk.outputs.cache-primary-key }}

      - name: Configure & Build library
        run: |
          export CCACHE_DIR="$HOME/.ccache"
          cmake -B build -S . \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_CLI=ON \
            -DBUILD_CUDA=OFF \
            -DENABLE_TESTS=OFF \
            -DENABLE_BENCHMARKS=OFF \
            -DITK_DIR=$HOME/itk-install/lib/cmake/ITK-5.4 \
            -DOpenMP_ROOT=$(brew --prefix libomp) \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build -- -j$(sysctl -n hw.ncpu)

      - name: Package
        run: |
          mkdir -p dolphin-macos
          cp build/frontends/cli/dolphin_cli dolphin-macos/
          chmod +x dolphin-macos/dolphin_cli
          # Bundle all dynamic dependencies and rewrite install names
          dylibbundler -b \
            -x dolphin-macos/dolphin_cli \
            -d dolphin-macos/libs \
            -cd \
            -p @executable_path/libs
          tar czf ${{ env.ARTIFACT_NAME }}.tar.gz dolphin-macos/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.tar.gz

  # -----------------------------------------------------------------
  # macOS — Test (clean VM, runtime deps only)
  # -----------------------------------------------------------------
  macos-test:
    strategy:
      fail-fast: false
    runs-on: macos-14
    needs: [prepare, macos]
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-macos-arm64

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Smoke test
        run: |
          tar xzf ${{ env.ARTIFACT_NAME }}.tar.gz
          chmod +x ./dolphin-macos/dolphin_cli
          output=$(./dolphin-macos/dolphin_cli --help 2>&1) || {
            echo "::error::dolphin_cli exited with code $?"
            echo "$output"
            exit 1
          }
          echo "$output" | grep -qE "psfgenerator|deconvolution" || {
            echo "::error::Unexpected help output — CLI did not start correctly"
            echo "$output"
            exit 1
          }
          echo "Smoke test passed."

  # -----------------------------------------------------------------
  # macOS — Publish to GitHub Release
  # -----------------------------------------------------------------
  macos-publish:
    strategy:
      fail-fast: false
    runs-on: macos-14
    needs: [prepare, macos-test]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-macos-arm64

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ARTIFACT_NAME }}.tar.gz

  # -----------------------------------------------------------------
  # Windows — Build (MSVC + vcpkg)
  # -----------------------------------------------------------------
  windows:
    runs-on: windows-2022
    needs: prepare
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-windows-x64
      VCPKG_ROOT: C:\vcpkg
      VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}\vcpkg-binary-cache

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Restore vcpkg binary cache
        id: vcpkg-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: vcpkg-windows-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            vcpkg-windows-

      - name: Create vcpkg binary cache directory
        shell: pwsh
        run: |
          New-Item `
            -ItemType Directory `
            -Force `
            -Path "$env:VCPKG_DEFAULT_BINARY_CACHE" |
          Out-Null

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Get CMake
        uses: lukka/get-cmake@v4.3.3

      - name: Configure
        shell: pwsh
        run: |
          cmake -B build -S . `
            -G "Ninja" `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_C_COMPILER=cl `
            -DBUILD_CLI=ON `
            -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT\scripts\buildsystems\vcpkg.cmake" `
            -DVCPKG_OVERLAY_PORTS="${{ github.workspace }}\vcpkg-overlay-ports" `
            -DBUILD_CUDA=OFF `
            -DENABLE_TESTS=OFF `
            -DENABLE_BENCHMARKS=OFF `
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}\installed"

      - name: Save vcpkg binary cache
        if: steps.vcpkg-cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: vcpkg-windows-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}

      - name: Build
        run: cmake --build build -j

      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dolphin-windows | Out-Null
          Copy-Item build/frontends/cli/dolphin_cli.exe dolphin-windows/
          $vcpkgBin = "build/vcpkg_installed/x64-windows/bin"
          if (Test-Path $vcpkgBin) {
            Copy-Item "$vcpkgBin\*.dll" dolphin-windows/
          }
          $vcRedist = "${env:VCToolsRedistDir}\x64\Microsoft.VC143.CRT"
          if (-not (Test-Path $vcRedist)) {
            throw "VCToolsRedistDir not found: $vcRedist"
          }
          Copy-Item "$vcRedist\*.dll" dolphin-windows/
          7z a $env:ARTIFACT_NAME.zip dolphin-windows/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.zip

  # -----------------------------------------------------------------
  # Windows — Test (clean VM, no build tools)
  # -----------------------------------------------------------------
  windows-test:
    runs-on: windows-2022
    needs: [prepare, windows]
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-windows-x64

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Smoke test
        shell: pwsh
        run: |
          7z --% x $env:ARTIFACT_NAME.zip -o./test
          $exe = "./test/dolphin-windows/dolphin_cli.exe"
          if (-not (Test-Path $exe)) {
            Write-Host "dolphin_cli.exe not found. Contents of ./test/dolphin-windows/:"
            Get-ChildItem ./test/dolphin-windows/ -ErrorAction SilentlyContinue | Format-Table Name, Length
            exit 1
          }
          $LASTEXITCODE = $null
          $output = (& $exe --help 2>&1) | Out-String
          $code = $LASTEXITCODE
          Write-Host "Exit code: $code"
          Write-Host "--- Output ---"
          Write-Host $output
          Write-Host "--- End output ---"
          if ($null -eq $code -or $code -ne 0) {
            Write-Error "dolphin_cli.exe exited with code $code"
            exit 1
          }
          if ($output -notmatch "psfgenerator|deconvolution") {
            Write-Error "Unexpected help output — CLI did not start correctly"
            exit 1
          }
          Write-Host "Smoke test passed."

  # -----------------------------------------------------------------
  # Windows — Publish to GitHub Release
  # -----------------------------------------------------------------
  windows-publish:
    runs-on: ubuntu-22.04
    needs: [prepare, windows-test]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-windows-x64

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ARTIFACT_NAME }}.zip

  # -----------------------------------------------------------------
  # Windows CUDA — Build (MSVC + vcpkg + CUDA Toolkit)
  # -----------------------------------------------------------------
  windows-cuda:
    runs-on: windows-2022
    needs: prepare
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-windows-x64-cu128
      VCPKG_ROOT: C:\vcpkg
      VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}\vcpkg-binary-cache

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install CUDA Toolkit
        uses: Jimver/cuda-toolkit@v0.2.35
        with:
          method: local
          cuda: '12.8.1'

      - name: Restore vcpkg binary cache
        id: vcpkg-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: vcpkg-windows-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            vcpkg-windows-

      - name: Create vcpkg binary cache directory
        shell: pwsh
        run: |
          New-Item `
            -ItemType Directory `
            -Force `
            -Path "$env:VCPKG_DEFAULT_BINARY_CACHE" |
          Out-Null

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Get CMake
        uses: lukka/get-cmake@v4.3.3

      - name: Configure
        shell: pwsh
        run: |
          cmake -B build -S . `
            -G "Ninja" `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_C_COMPILER=cl `
            -DBUILD_CLI=ON `
            -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT\scripts\buildsystems\vcpkg.cmake" `
            -DVCPKG_OVERLAY_PORTS="${{ github.workspace }}\vcpkg-overlay-ports" `
            -DBUILD_CUDA=ON `
            -DENABLE_TESTS=OFF `
            -DENABLE_BENCHMARKS=OFF `
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}\installed"

      - name: Save vcpkg binary cache
        if: steps.vcpkg-cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: vcpkg-windows-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}

      - name: Build
        run: cmake --build build -j

      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dolphin-windows-cuda | Out-Null
          Copy-Item build/frontends/cli/dolphin_cli.exe dolphin-windows-cuda/
          $vcpkgBin = "build/vcpkg_installed/x64-windows/bin"
          if (Test-Path $vcpkgBin) {
            Copy-Item "$vcpkgBin\*.dll" dolphin-windows-cuda/
          }
          $vcRedist = "${env:VCToolsRedistDir}\x64\Microsoft.VC143.CRT"
          if (-not (Test-Path $vcRedist)) {
            throw "VCToolsRedistDir not found: $vcRedist"
          }
          Copy-Item "$vcRedist\*.dll" dolphin-windows-cuda/
          # Copy CUDA runtime DLLs from the toolkit installation
          $cudaBin = "${env:CUDA_PATH}\bin"
          if (-not (Test-Path $cudaBin)) {
            throw "CUDA bin directory not found: $cudaBin"
          }
          Copy-Item "$cudaBin\cudart64_*.dll" dolphin-windows-cuda/
          Copy-Item "$cudaBin\cufft64_*.dll" dolphin-windows-cuda/
          Copy-Item "$cudaBin\cublas64_*.dll" dolphin-windows-cuda/
          Copy-Item "$cudaBin\cublasLt64_*.dll" dolphin-windows-cuda/
          7z a $env:ARTIFACT_NAME.zip dolphin-windows-cuda/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.zip

  # -----------------------------------------------------------------
  # Windows CUDA — Test (clean VM, no build tools)
  # -----------------------------------------------------------------
  windows-cuda-test:
    runs-on: windows-2022
    needs: [prepare, windows-cuda]
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-windows-x64-cu128

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Smoke test
        shell: pwsh
        run: |
          7z --% x $env:ARTIFACT_NAME.zip -o./test
          $exe = "./test/dolphin-windows-cuda/dolphin_cli.exe"
          if (-not (Test-Path $exe)) {
            Write-Host "dolphin_cli.exe not found. Contents of ./test/dolphin-windows-cuda/:"
            Get-ChildItem ./test/dolphin-windows-cuda/ -ErrorAction SilentlyContinue | Format-Table Name, Length
            exit 1
          }
          $LASTEXITCODE = $null
          $output = (& $exe --help 2>&1) | Out-String
          $code = $LASTEXITCODE
          Write-Host "Exit code: $code"
          Write-Host "--- Output ---"
          Write-Host $output
          Write-Host "--- End output ---"
          if ($null -eq $code -or $code -ne 0) {
            Write-Error "dolphin_cli.exe exited with code $code"
            exit 1
          }
          if ($output -notmatch "psfgenerator|deconvolution") {
            Write-Error "Unexpected help output — CLI did not start correctly"
            exit 1
          }
          Write-Host "Smoke test passed."

  # -----------------------------------------------------------------
  # Windows CUDA — Publish to GitHub Release
  # -----------------------------------------------------------------
  windows-cuda-publish:
    runs-on: ubuntu-22.04
    needs: [prepare, windows-cuda-test]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      ARTIFACT_NAME: dolphin-${{ needs.prepare.outputs.version }}-windows-x64-cu128

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ARTIFACT_NAME }}.zip
